export default function ForgotPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <title>Social Media - Forgot password</title>
      {children}
    </div>
  );
}
